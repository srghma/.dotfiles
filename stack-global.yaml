templates:
  params:
    author-name: srghma
    author-email: srghma@gmail.com
    license: Apache-2.0
    github-username: srghma

# system-ghc: true
# install-ghc: false # Prevents Stack from trying to install GHC itself

nix:
  enable: true
  # pure: true
  # packages: [glpk, pcre, curl,  zlib, ncurses, gmp]
  # there are two options to use nix with stack: `packages` option, `shell-file` option
  # shell-file: shell.nix
  # shell-file: ~/.dotfiles/stack-shell.nix
